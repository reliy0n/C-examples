#include <stdio.h>

  int main(){
    FILE *fPtr;
    fPtr = fopen("payables.dat","r")
    fprintf(fPtr, "%d%s%d\n", account, company, amount);

    fopen("receive.dat", "r+");
    fscanf(recPtr, "%d%s%d\n", &account, company, &amount);

    if ( tfPtr = fopen("tools.dat", "a") ) != NULL)){}
    if ( cfPtr = fopen("courses.dat", "a+")) != NULL){}
  }
